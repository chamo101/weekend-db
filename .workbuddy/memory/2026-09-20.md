# weekend-db 排查与决策记录

## 2026-09-20 · 共建表链接「文档不存在」修复

**症状**：大人反馈 9/9 创建的腾讯文档共建表链接打开显示「文档不存在」。
项目 weekend-db（休着·双休公司数据库）自 9/13 后未推进，属停摆状态。

**结论：文档根本没被删，是链接 ID 抄错了大小写。**

```
官方（manage.query_file_info 返回的 url）：…/smartsheet/DSUZrY0J L blpjbE96
代码/README/记忆里用的：                  …/smartsheet/DSUZrY0J l blpjbE96
```

区分大小写，差一个 `L`→`l` 就整页白屏。

**证据链**
- `manage.query_file_info` → `status: normal`、`title: 休着·双休公司数据库(共建表)`、`total 141`
- `manage.get_privilege` → `policy: 3`（所有人可编辑，本来就是对的）
- curl 对比：大写 L 页面 452KB 且能解出真实标题与 `authInfo`；小写 l 仅 63KB、`{"padData":{"blankPageType":20}}`
- 同步自动化 9/13 23:30 记录「共建表141条」正常 → 说明 9/13 前文档一直是好的（脚本用 file_id，不受影响）

**修复范围（4 处代码 + 1 处记忆）**
- `index.html` ×2 · `README.md` ×1 · `miniprogram/utils/config.js` ×1 · `Claw/.workbuddy/memory/2026-09-09.md` ×1
- commit `c808c19`，已 push；GitHub Pages 约 45s 后生效（复查用 `?v=$(date +%s)` 绕缓存）

**顺带加固**
- `scripts/sync_smartsheet.py` 原本写死插件版本目录 `5.5.4-wb.…`，插件已升到 5.5.6，旧目录迟早被清理 →
  改为 `_find_tdoc_skill()` 自动列目录 + 按数字段排序取最新，已验证解析到 5.5.6 且脚本正常（141 条，0 新增）
- 新增 `TDOC_URL` 常量集中放正确链接，附大小写注释。commit `f03708b`

**自动化**：`70f85514-986c-44e9-9ffb-0202471818e5` 仍 ACTIVE（每周日 23:30，cwd=weekend-db），未受影响。

**教训**：腾讯文档分享链接 ID **区分大小写**，且拼错的表现就是「文档不存在」，与真删除无法目视区分。
以后凡要引用腾讯文档链接，一律先 `manage.query_file_info` 拿官方 `url`，不要手抄。
排查方法已固化为 skill `tencent-docs-link-recovery`。
